//A Program that takes in two words as an argument and returns true
//if they are anagrams and false if they arent.
function anagram(word1, word2){
    if (typeof word1 != "string" && typeof word2 != "string"){
        return false;
    }
    for(var i=0; i<word1.length; i++){
        
    }
}